This project contains some C++ experiments.


Running And Testing
===================

Use `make run-%` to run a target and `make test-%` to test such a target. To see
all available targets (to substitute for `%`), use `make show-targets`.

Use `make new NAME="foo"` to create a new experiment.


License
=======

MIT license.
